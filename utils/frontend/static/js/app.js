// --- MAIN APPLICATION INITIALIZATION ---

// Exposed initialization function
function initApp() {
    // Initialize DOM references
    initializeElements();

    // Initial render
    renderAll();
}
